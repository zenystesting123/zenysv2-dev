import { DocumentsettingsDirective } from './documentsettings.directive';

describe('DocumentsettingsDirective', () => {
  it('should create an instance', () => {
    const directive = new DocumentsettingsDirective();
    expect(directive).toBeTruthy();
  });
});
